{% from "partials/macro.twig" import interfaceAttributes %}
import { ValueObject } from '@pocket-architect/core';

export interface {{valueObject.interface.name}} {
{{- interfaceAttributes(valueObject.attributes)}}
}

export
class {{valueObject.name}} extends ValueObject<{{valueObject.interface.name}}> {
  public static create(props: {{valueObject.interface.name}}): {{valueObject.name}} {
    return new {{valueObject.name}}(props);
  }
}
