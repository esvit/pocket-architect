import { {{entity.service.name}} } from './{{entity.service.name}}';

describe('{{entity.service.name}}', () => {
  const {{ entity.repository.name }} = {
    load: jest.fn(),
    save: jest.fn(),
    remove: jest.fn(),
  };
  const service = new {{entity.service.name}}({
    {{ entity.repository.name }}
  });

  test('load', async () => {
    expect(service).toBeDefined();
  });
});
