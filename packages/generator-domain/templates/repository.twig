{% from "partials/macro.twig" import interfaceAttributes, enums %}
{{- enums(repository.attributes) }}
export interface {{repository.interface.name}} {
{{interfaceAttributes(repository.attributes)}}
}
